const path = require("path");
require("dotenv").config();

module.exports = {
  config: path.resolve("src/infra/config/sequelize-config.js"),
  "migrations-path": path.resolve("src/infra/migrations"),
};
